<% provide(:title, t("users.index.title")) %>

<h1 class="mb-4"><%= t("users.index.title") %></h1>

  <%= will_paginate @users %>
<ul class="users">
    <%= render @users %>
</ul>

<%= will_paginate @users %>
