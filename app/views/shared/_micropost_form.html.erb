<%= form_with model: @micropost, local: true do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t("microposts.form.placeholder"), class: "form-control" %>
    <%= label_hint(f, :content) %>
  </div>
  <div class="mb-2">
    <%= f.file_field :picture, accept: "image/jpeg,image/gif,image/png", class: "form-control" %>
    <%= label_hint(f, :picture) %>
  </div>
  <div class="d-grid">
    <%= f.submit t("microposts.form.submit"), class: "btn btn-primary" %>
  </div>
<% end %>


<script type="text/javascript">
  $('#micropost_picture').bind('change', function() {
    var size_in_megabytes = this.files[0].size/1024/1024;
    if (size_in_megabytes > 5) {
      alert('<%= j(t("microposts.form.file_size_error")) %>');
    }
  });
</script>
